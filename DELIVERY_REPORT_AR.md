# تقرير تسليم تحديث موقع Murshid

## النتيجة التنفيذية
تم تحديث مستودع `qawsdegyu/murshidpoly` ورفع commit رقم `eeae7cf` إلى الفرع `main`. كما تم تحديث البيانات الحية في مشروع Supabase `uvuflufejfhlsqtxqemy` وتنظيف ملفات Drive المرتبطة.

## ما تم إنجازه

| المجال | الإجراء | النتيجة |
|---|---|---|
| أسماء الموارد | مقارنة أسماء Supabase بأسماء ملفات Drive وتحديث السجلات المطابقة | تحديث 4878 موردًا مطابقًا |
| روابط Drive | توحيد الروابط إلى روابط تنزيل Drive الحالية | 5056 رابط Drive بقيت في قاعدة البيانات بعد التنظيف |
| الوصول العام | إضافة صلاحية `anyone / reader` للملفات غير العامة | إتاحة 85 ملفًا مطابقًا للقراءة عبر الرابط، دون صلاحية تحرير |
| التكرارات | مقارنة الاسم المنظف، المادة، الامتداد، نوع الملف، الحجم ووقت التعديل | حذف 14 سجلًا مكررًا ونقل 14 نسخة Drive الأضعف إلى المهملات |
| التخصصات | الإبقاء على جميع التخصصات الحالية وعدم حصرها في ثلاثة | تصحيح alias قديم `comp` إلى `computer` في حساب واحد |
| الفلترة | إضافة تطبيع aliases مثل `comp`, `cs`, `cne`, `cybersecurity` وربطها بالقيم القياسية | فلترة الخزانة حسب تخصص الحساب مع إبقاء المقررات `common` |
| صفحة المادة | تصحيح قراءة `credit_hours` وإضافة fallback للأسماء العربية والإنجليزية | عرض أدق لبيانات المقرر |

## حالة البيانات بعد التنظيف
أصبح عدد سجلات `resources` في Supabase هو 5083 سجلًا، منها 5056 مرتبطة بروابط Drive. بقيت 27 موردًا غير مرتبطة بـ Drive ضمن البيانات الحالية، ولم أستبدلها بتخمينات أو ملفات من مواقع خارجية.

تمت مراجعة المواقع المرجعية الثلاثة. موقع لجنة سنافر يعرض مواد مشتركة ومخططًا هندسيًا، وموقع لجنة الميكانيك يعرض تخصصات الميكانيك والأوتوترونكس والآلات الحرارية والهيدروليكية، بينما يعرض CNE Family مواد الحاسوب والشبكات والأمن السيبراني. استُخدمت هذه المواقع كمرجع لفهم التسميات فقط، ولم يتم نسخ ملفات أو محتوى إليها تلقائيًا.

## التحقق البرمجي
نجح `npm run build` بالكامل، ونجح `git diff --check`. أما الاختبارات فنجحت في اختبارات الكيمياء والحاسوب والاتصالات والميكاترونكس والحرارة والأمان، بينما بقيت ثلاثة اختبارات قديمة في `src/test/gpa.test.ts` فاشلة بسبب اختلافات في منطق حساب المعدل (`3.625` بدل `3.5` و`3.275` بدل `3.3`). هذه الإخفاقات لا ترتبط بتحديث Drive أو فلترة التخصصات، لكنها تحتاج إصلاحًا مستقلًا قبل اعتبار حزمة الاختبارات كلها خضراء.

## حالة النشر
المشروع في Vercel مرتبط فعليًا بمستودع GitHub، وقد التقط commit الجديد، لكن نشر الإنتاج ظهر بحالة `BLOCKED` بسبب إعداد حساب/تعاون في Vercel، وليس بسبب فشل البناء. رابط التشخيص الذي أرجعه Vercel هو: https://vercel.com/docs/deployments/troubleshoot-project-collaboration#account-configuration

رابط المعاينة المرتبط بالنشر المحجوب: https://murshidpoly-nayf-cecuzvw4i-mocvskhfssr-3411s-projects.vercel.app

## ملاحظات مهمة
الملفات التي لم تُعثر عليها في جرد Drive لم تُحذف من Supabase، حتى لا تضيع روابط قديمة قبل التأكد من مصدر بديل. كما أن نقل النسخ المكررة إلى المهملات في Drive قابل للاستعادة من سلة المهملات خلال فترة الاحتفاظ لدى Google Drive.

الخطوة اليدوية الوحيدة المتبقية هي معالجة حالة `BLOCKED` في لوحة Vercel من إعدادات الحساب/التعاون، ثم إعادة النشر أو السماح للنشر التلقائي بالاستمرار. بعد ذلك يجب إصلاح اختبارات GPA القديمة إذا كان المطلوب أن تكون كل الاختبارات ناجحة.

## تفسير Vercel الرسمي لحالة Blocked
وفقًا لتوثيق Vercel الرسمي، فإن فريق Hobby لا يدعم التعاون في المستودعات الخاصة، ويشترط أن يكون مؤلف commit هو مالك فريق Hobby وأن يكون حساب GitHub مرتبطًا بحساب Vercel. كما يذكر التوثيق أن عدم تطابق بريد commit مع بريد موثّق في GitHub قد يمنع إسناد commit إلى صاحبه. المصدر: https://vercel.com/docs/deployments/troubleshoot-project-collaboration#account-configuration

## فحص إعدادات الحساب
تم فتح https://vercel.com/account/settings/authentication، وظهرت صفحة تسجيل الدخول بدل صفحة إعدادات الحساب. لذلك لا يمكنني رؤية حالة GitHub أو ربطه نيابةً عن المستخدم من هذه الجلسة دون تسجيل دخول يدوي.

## تحقق النشر بعد تعطيل الحماية
بعد تعطيل Vercel Authentication، أصبحت نسخة المعاينة تفتح صفحة Hadeed مباشرة بدل صفحة تسجيل الدخول إلى Vercel. التحقق النصي أظهر المسارات الرئيسية مثل الخزانة والتخصصات والمدرسين وحاسبة المعدل ولوحة الجدول. تعذّر العثور على نص التذييل في الاستخراج النصي للصفحة الرئيسية، لذلك يحتاج ظهوره المرئي إلى مراجعة مباشرة داخل المتصفح بعد اكتمال تحميل الصفحة أو فتح صفحة داخلية مثل `/vault`.

## تحديث فحص لوحة الإدارة والنشر
تمت مراجعة المسارات الرئيسية ولوحة الإدارة، وتبيّن أن الإعلانات كانت تحفظ `is_global` و`target_major` في قاعدة البيانات، لكن لوحة الإدارة لم تكن تعرض حقل اختيار التخصص. أُضيف الآن اختيار لجميع التخصصات الموجودة، مع قاعدة واضحة: الإعلان العام يظهر للجميع، والإعلان غير العام يظهر للتخصص المحدد فقط. كما وُحّدت aliases التخصصات في المواد والإعلانات.

تم إضافة تذييل `Powered by Oberix` في التخطيط العام، بحيث تكون كلمة Oberix وحدها رابطًا إلى https://www.operixsys.online/ في تبويب جديد. نجح البناء بعد التعديلات. آخر commit هو `1e708ea`، وآخر نشر Vercel المرتبط به أصبح `READY` على: https://murshidpoly-nayf-3s90yyud2-mocvskhfssr-3411s-projects.vercel.app

تم تعطيل Vercel Authentication الخارجي بعد تأكيد المستخدم، وأصبح الموقع يفتح للطلاب مباشرة، مع بقاء حماية الصفحات الداخلية الخاصة بتسجيل الدخول في Murshid كما هي.

لا تزال أداة lint تعرض 269 خطأً قديمًا و59 تحذيرًا موزعة على ملفات المشروع، كما أن ثلاثة اختبارات GPA قديمة تفشل بسبب اختلاف منطق الحساب؛ هذه لا تمنع نجاح build أو نشر الواجهة، لكنها أعمال صيانة مستقلة إذا كان المطلوب تنظيف المشروع بالكامل من كل ملاحظات lint والاختبارات.
