# تقرير تقوية حماية Murshid

## نطاق العمل

تم تنفيذ مراجعة دفاعية للمستودع بهدف تقليل سطح التعرض، حذف النسخ القديمة والملفات التجريبية غير المستخدمة، وإضافة إعدادات حماية مناسبة للنشر على Vercel. لم يتم تعديل بيانات Supabase أو حذف أي ملفات مستخدمة في التشغيل الحالي.

## ما تم تنفيذه

تم حذف مجلد المشروع التجريبي القديم `my-project`، ومجلد `src/scratch` الذي كان يحتوي على أدوات فحص ورفع وتجارب، ونسخ الصفحات القديمة مثل `MajorPage_*` و`BuildingsPage_v2` و`SchedulePlanner_v2`. كما حُذفت سكربتات المعالجة لمرة واحدة الموجودة في جذر المشروع، لأنها ليست ضمن مسار البناء أو التشغيل.

تمت إضافة قواعد إلى `.gitignore` لمنع إدخال مخرجات التحليل والملفات المؤقتة مستقبلًا. أظهر فحص الملفات المتتبعة وجود `.env.example` فقط، ولم يجد مفاتيح خدمة أو مفاتيح خاصة أو رموز وصول فعلية. توجد إشارة نصية تعليمية إلى `SUPABASE_SERVICE_ROLE_KEY` داخل ملف تجريبي تم حذفه، ولم تكن قيمة سرية.

تمت إضافة رؤوس حماية عامة إلى `vercel.json`: `Strict-Transport-Security`، و`X-Content-Type-Options`، و`X-Frame-Options`، و`Referrer-Policy`، و`Permissions-Policy`. لم تتم إضافة Content-Security-Policy عشوائية حتى لا تتسبب في تعطيل Supabase أو الصور والروابط الخارجية قبل إعداد قائمة مصادر دقيقة.

كشف فحص تبعيات الإنتاج عن ثغرتين متوسطتي الخطورة في `react-router` الإصدار 6.30.6. تم تحديث `react-router-dom` إلى `7.18.2`، ثم نجح فحص TypeScript وبناء Vite.

## التحقق

نجح `git diff --check`، ولم تظهر مراجع برمجية للنسخ القديمة المحذوفة، ونجح بناء الواجهة. بقي تحذير حجم حزمة موجود مسبقًا ولا يمثل فشلًا أمنيًا أو وظيفيًا. لم يتم إجراء اختبار اختراق هجومي؛ هذا التقرير يصف تقوية دفاعية وتنظيفًا آمنًا للمستودع.

## ملاحظات تشغيلية

يجب الاحتفاظ بمفاتيح Supabase الخاصة في إعدادات Vercel أو Supabase فقط، وعدم وضعها داخل `src` أو `public`. مفتاح Supabase العام المستخدم في الواجهة ليس بديلًا عن سياسات RLS؛ يجب إبقاء RLS مفعّلًا على الجداول الحساسة.
