# ملاحظات مصدر جريدة المواد الرسمي

المصدر: http://appserver.fet.edu.jo:7778/courses/index.jsp

## بنية المصدر
الصفحة تحتوي على قوائم الدرجة العلمية والكلية والقسم، ثم زر بحث. القوائم تُحمّل من خلال JavaScript عبر طلبات POST متزامنة إلى `/courses/actions/rmiMethod`.

## نقاط النهاية المكتشفة
- `getDegrees()` لجلب الدرجات.
- `getColleges()` لجلب الكليات.
- `getDepartments(collegeId)` لجلب الأقسام.
- `getCoursesPagesCount(degreeId, collegeId, departmentId)` لمعرفة عدد صفحات النتائج.
- `getCourses(degreeId, collegeId, departmentId, pageNo)` لجلب صفوف المواد والشعب.

## حقول الصفوف
كل صف يعيد على الأقل: `no`, `name`, `hours`, `sectionNo`, `times`, `lecturers`, `rooms`, `status`, `remarks`.

## حالات الشعب
حسب JavaScript الرسمي: الحالة 1 = متاحة، الحالة 2 = ملغاة، الحالة 3 = مغلقة. يجب ألا يعرض منسق الجداول الشعب ذات الحالة 2 أو 3 على أنها مفتوحة.

## ملاحظة تقنية
الصفحة الرسمية تستخدم ترميزًا عربيًا قديمًا يظهر مشوهًا عند طلبه بترميز UTF-8، لذلك يجب اختبار فك الترميز قبل تخزين أسماء المواد والشعب. كما أن المصدر لا يظهر واجهة API عامة موثقة؛ المزامنة ستحتاج خادمًا وسيطًا/وظيفة خلفية تستدعي RMI، لأن المتصفح لا ينبغي أن يعتمد على طلبات cross-origin مباشرة.
